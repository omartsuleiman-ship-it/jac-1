module.exports = async function () {
  // مقصود يفضل بسيط كده — التطبيق ده مش بيعرض أي أزرار تحكم على شاشة
  // القفل، فمفيش أحداث remote-control محتاجين نتعامل معاها هنا. الملف ده
  // لازم يكون موجود ومسجّل بس عشان react-native-track-player يقدر يشغّل
  // جلسة الصوت في الخلفية أصلاً.
};